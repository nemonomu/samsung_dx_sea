# Walmart TV

Run `run_walmart_tv_pipeline.bat` on the RDP machine.

This category currently collects Walmart TV listing, detail, review, normalizes the DB insert shape, validates it 3 times, and commits to DB only after validation passes.
